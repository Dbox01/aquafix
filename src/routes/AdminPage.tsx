/** Placeholder for Mendix Admin_Overview. User management arrives with slice 0's UI work. */
export function AdminPage() {
  return (
    <section className="space-y-2">
      <h1 className="text-xl font-semibold">Admin</h1>
      <p className="text-sm text-slate-600">User management is not built yet.</p>
    </section>
  );
}
