# Copy to .env.local for local development.
#
# In Vercel these are set in the dashboard instead (Project Settings ->
# Environment Variables), because .env.local is gitignored and never pushed.
#
# The anon key is PUBLIC by design -- it ships inside the frontend bundle, and
# Row Level Security is what protects the data. Safe to share.
#
# There is deliberately no service_role key here. It bypasses every policy and
# must never reach client code or version control.

VITE_SUPABASE_URL=https://nmstuuccmbojdumpvimn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5tc3R1dWNjbWJvamR1bXB2aW1uIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODU0NzkyMDgsImV4cCI6MjEwMTA1NTIwOH0.30hN1161BeyWUlrYZB9s7xPP0feo3a7Z5pLx_DnCIfA
